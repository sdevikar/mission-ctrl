
anxiety reduction before testing --> how to avoid avoidance without getting bogged down by the tests
triage for features -> help user distill it

what type of markdown files do we need
how does the worrkflow suggest you might need to do x before y --> identify gaps --> but gaps must be relevant to the mission and the style of development
how can we prevent the agent from inundating the developer with too many decisions? --> this is because the human in the loop would mean being involved in every little decision --> this can get frustrating

should we assign roles to the agent

how to effectively catch the user up 
	--> summary should tell user what they have done so far and what features are covered. what bugs / blockers they have
		--> e.g. last we worked on this, we stopped at x

mcp server for mermaid rendering?

waterfall vs agile? what's after agile

what components are you thinking of for this project? OR agent itself suggests what components. And then it can say -- well no, as per industry standards, you should be doing this

-- really like clicking the node and implement idea
	--> maybe nodes should be disabled or enabled based on readiness

-- need to balanace endless discussion with incremental implementation
	-- that means, agent should always keep track of what specs are ready to implement



	implementation plan

	- get specs from the chat in a digestible format that can be each completed within 45 minutes

should capture git history as well (for recap purposes)